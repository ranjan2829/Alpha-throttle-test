# Throttle tickets

Live bursts write one numbered file per PR (`0001.md`, `0002.md`, …). Each file is a single line. Volume is the test; the diff stays tiny.

Dry-run writes the same files under `.alpha/throttle/tickets/` and never pushes them.
